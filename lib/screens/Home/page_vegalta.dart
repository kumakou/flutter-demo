import 'package:flutter/material.dart';

class Page_Vegalta extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "ベガルタ",
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
